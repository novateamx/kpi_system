spring:
  application:
    name: kpi-system
  sql:
    init:
      platform: postgres
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/kpi_db
    username: postgres
    password: bexa
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      port: 6379
      host: localhost

server:
  port: 7777

jwt:
  token:
    secret: 3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b
auth:
  white-list:
    - "/auth/login"
    - "/auth/register"